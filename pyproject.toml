[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "object-generator-core"
version = "0.7.0"
description = "Software-independent generation of editable 3D objects"
requires-python = ">=3.7"
dependencies = []
license = {file = "LICENSE"}

[tool.setuptools.packages.find]
include = ["object_core*", "blender_adapter*", "humanoid_blender*"]
