[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "humanoid-core"
version = "0.5.0"
description = "Software-independent inputs for editable humanoid generation"
requires-python = ">=3.7"
dependencies = []
license = {file = "LICENSE"}

[tool.setuptools.packages.find]
include = ["humanoid_core*", "humanoid_blender*"]
