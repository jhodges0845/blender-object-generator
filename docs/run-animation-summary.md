# Run animation implementation summary

This branch adds Human 1.0 and Dog Run clips as generated, editable, in-place animation actions. Both providers expose Run capability, Blender can generate/select the clip, and exports treat it like the existing Idle and Walk library entries.
