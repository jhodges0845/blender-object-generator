# Run animation implementation summary

Human and Quadruped expose generated, editable, in-place Run actions. Both providers advertise Run capability, Blender can generate/select the clip, and exports treat it like the existing Idle and Walk library entries.
