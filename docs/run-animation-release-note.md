# Run animation release note

Human and Quadruped support generated Run clips. Run appears in Blender's Animations workflow, exports with the rest of the generated clip library, and defaults to the engine-facing name `Run`.
